<!DOCTYPE html>
<html>
<head>
{% set title = "SkyBlock Manager " + version %}
<title>{{ title }}</title>
<script type="text/javascript" src="{{ url_for('static', filename='jquery-1.7.1.min.js') }}"></script>
<script type="text/javascript" src="{{ url_for('storejs') }}"></script>
<link rel="stylesheet" type="text/css" href="{{ url_for('static', filename='style.css') }}" />
</head>
<body>
<div id="bgdiv">
<div id="content">
<div id="responsediv"></div>
<h1>{{ title }}</h1>
<form name="dummy" onchange="javascript:store()">
<h2>Challenges (<span id="completedcount">{{ completed }}</span>/{{ total }} completed):</h2>
<ul>
{% for c in challenges %}
<li>
    {% if c.has_amount %}
    <input type="text" class="amnt" name="{{ c.id }}" id="data{{ c.id }}" value="{{ c.current_amount }}" />/<b>{{ c.amount }}</b>
    {% else %}
    <input type="checkbox" name="{{ c.id }}" id="data{{ c.id }}" class="cb" {%if c.completed%}checked="checked"{%endif%} />
    {% endif %}
    <span id="text{{ c.id }}" {% if c.completed %}class="completed"{% endif %}>{{ c }}</span>
</li>
{% endfor %}
</ul>
</form>
</div></div>
</body></html>
